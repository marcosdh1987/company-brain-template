# Changelog

This repo uses semver tags (`vX.Y.Z`), same as the harness. Rule of thumb:
`MINOR` for new sections or runbooks, `PATCH` for content fixes, `MAJOR` for
folder restructures that break consumer repos' adapter links.

## [0.1.0]

### Added
- Initial company brain structure: `brain/` (domain, decisions, glossary,
  conventions, architecture, runbooks, team), `memory/`, multi-tool adapters,
  bootstrap and maintenance skills, init and validation scripts.
