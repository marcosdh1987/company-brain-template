# Onboarding a new person (or agent)

- **When to use it:** someone joins the engagement.
- **Owner:** _PENDING_
- **Risk:** low
- **Access required:** git hosting, AI gateway, channels.

## Steps

1. Access: repos, team channel, secrets manager. _PENDING: exact list._
2. Read this brain: `AGENTS.md` first, then `00-context/` (30 minutes well
   spent).
3. If the org has code repos: clone this brain, run `make workspace` — every
   registered repo lands as a sibling (see `docs/workspace.md`).
4. Configure the AI assistant: gateway token from the platform owner; the
   harness setup guide lives in each code repo.
5. First task: a small, well-defined item worked with the harness loop
   (Ground → Plan → Delegate → Verify → Compound).

## Verification

- The person can answer a domain question from the brain without asking, and
  ran the assigned repo's gates green.

## Rollback

- Not applicable.
