# Onboarding a new team member

- **When to use it:** a dev joins (or an agent is configured for the first time).
- **Owner:** _PENDING_
- **Risk:** low
- **Access required:** GitHub org invitations, AI gateway, channels.

## Steps

1. Access: GitHub org, team channel, secrets manager. _PENDING: the
   organization's exact list._
2. Read this brain starting at `brain/00-index.md` (30 minutes well spent).
3. Configure the AI assistant: request a gateway token from the platform owner;
   follow the harness setup guide in the assigned project's repo.
4. Clone the assigned project's repo and run its local onboarding
   (`make install` or equivalent; see the repo's README).
5. First task: an issue labeled `good-first-issue`, worked with the harness
   working loop (Ground → Plan → Delegate → Verify → Compound).

## Verification

- The person can run the assigned repo's gates green and merged their first PR.

## Rollback

- Not applicable.
