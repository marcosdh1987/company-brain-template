# Runbooks

> Step-by-step operational procedures, written so a newcomer — or an agent —
> can follow them. One runbook per file, named after the action
> (`deploy-x.md`, `rotate-credentials.md`). Copy `template.md` to create one.
>
> Rule: if something was explained twice over chat, it becomes a runbook.

| Runbook | When it is used | Owner |
|---|---|---|
| `onboarding-dev.md` | A new person joins the team | _PENDING_ |
